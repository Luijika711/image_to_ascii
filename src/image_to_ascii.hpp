#include <iostream>
#include<CImg.h>
class image_to_ascii
{
private:
public:
    image_to_ascii(std::string filepath)
    {
        std::cout << "started image to ascii on " + filepath;
    }
    void app()
    {
    }
};